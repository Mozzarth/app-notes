{
  "name": "notedev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=dev ts-node-dev  --cls --ignore-watch node_modules ./src/server/index.ts",
    "build": "npm run build:clean && npm run build:tsc && npm run build:dist",
    "build:clean": "rm -r dist; exit 0",
    "build:tsc": "tsc -p tsconfig.prod.json",
    "build:dist": "copy 'src/**/*.{json,yaml,html,png,env}' dist/src"
  },
  "author": "Mozzarth",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/morgan": "^1.9.2",
    "@types/nodemailer": "^6.4.0",
    "@types/uuid": "^8.3.0",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "mocha": "^8.2.1",
    "ts-node-dev": "^1.1.1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "helmet": "^4.4.1",
    "morgan": "^1.10.0",
    "mssql": "^6.3.1",
    "nodemailer": "^6.4.17",
    "typescript": "^4.1.3",
    "uuid": "^8.3.2"
  }
}
